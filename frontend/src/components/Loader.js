import React from 'react'
import { BeatLoader } from 'react-spinners'

const Loader = () => {
    return (
        <div>
            <BeatLoader size={48} loading />
        </div>
    )
}

export default Loader
